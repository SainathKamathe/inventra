# System Design

- React SPA frontend built with Vite and styled by Tailwind.
- FastAPI backend exposing REST endpoints under `/api`.
- Axios service layer used by frontend to call backend.
- Modular agents/services/routes structure for easy scaling.
